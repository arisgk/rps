import React from 'react';

const styles = {
  container: {
    flex: 'auto',
    backgroundColor: '#f5f5f5',
  },
};

const Game = () => (
  <div style={styles.container} />
);

export default Game;
